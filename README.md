# C-Dynamic-Strings
Library in C for dynamic string management, providing a custom String structure with safe memory handling and support for common operations such as insert, set, push, find, sort, and resize with automatic buffer management.
